name: Docker

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  docker:
    uses: czerwonk/go-ci/.github/workflows/docker-publish.yml@v5
    with:
      platforms: linux/amd64
